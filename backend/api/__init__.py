"""FastAPI factory + routes (Phase 2 scaffolding — intentionally empty).

Phase 2 does not touch root-level `ui_server.py`. Phase 4 (Next.js
frontend) is when this package begins hosting a slim FastAPI
replacement: `app.py`, `routes/`, `middleware/`.

Until then, this package is a placeholder so the target layout from
ARCHITECTURE.md §13 is visible in the repository.
"""
